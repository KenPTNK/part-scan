# The CNN runs as TFLite via the lightweight LiteRT runtime — full TensorFlow
# segfaults on Streamlit Cloud's container and is not needed for inference.
# (Locally, if ai-edge-litert has no wheel for your OS, the app falls back to
# tensorflow's bundled tf.lite.Interpreter if TensorFlow is installed.)
streamlit==1.58.0
streamlit-webrtc==0.74.2
ai-edge-litert
ultralytics==8.4.24
opencv-python==4.13.0.92
numpy
pillow
google-genai==1.75.0
python-dotenv==1.2.2
