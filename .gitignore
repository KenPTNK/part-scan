# secrets — never commit
.env
.streamlit/secrets.toml

# local Keras checkpoints — the repo ships the TFLite conversion instead
*.keras

# python
__pycache__/
*.pyc

# training report images (not used by the deployed app)
confusion_matrix.png
F1_curve.png
P_curve.png
R_curve.png
PR_curve.png
results.png
